name: Lint and Test
on:
  pull_request:
    branches:
      - master
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Running lint
        run: npm run lint
      - name: Running test
        run: npx ng test
